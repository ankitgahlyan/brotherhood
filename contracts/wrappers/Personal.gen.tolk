/// Auto-generated wrapper for contract 'Personal'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/personalMinter/storage"

/// Storage `PersonalStore` is defined in `@contracts/personalMinter/storage`
struct Personal {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Personal.fromStorage(
    storage: PersonalStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Personal {
    val stateInit = ContractState {
        code: build("PersonalMinter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Personal { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Personal.fromAddress(address: address): Personal {
    return Personal { address };
}

/// Deploys the contract to the blockchain
fun Personal.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendMintNewJettons(
    self,
    from: address,
    queryId: uint64,
    mintRecipient: address,
    tonAmount: coins,
    internalTransferMsg: InternalTransferStep,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: MintNewJettons {
            queryId,
            mintRecipient,
            tonAmount,
            internalTransferMsg: internalTransferMsg.toCell(),
        },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendNotifyMinter(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    burnInitiator: address,
    sendExcessesTo: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: NotifyMinter { queryId, jettonAmount, burnInitiator, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendRequestWalletAddress(
    self,
    from: address,
    queryId: uint64,
    owner: address,
    includeOwnerAddress: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RequestWalletAddress { queryId, owner, includeOwnerAddress },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendChangeMinterAdmin(
    self,
    from: address,
    queryId: uint64,
    newAdminAddress: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeMinterAdmin { queryId, newAdminAddress },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendChangeMinterMetadata(
    self,
    from: address,
    queryId: uint64,
    newMetadata: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ChangeMinterMetadata { queryId, newMetadata },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendUpgrade(
    self,
    from: address,
    walletUpgrade: bool,
    walletVersion: uint10,
    sender: address,
    newData: cell?,
    newCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Upgrade { walletUpgrade, walletVersion, sender, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendHotUpgrade(
    self,
    from: address,
    additionalData: cell?,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HotUpgrade { additionalData, code },
    });
    return net.send(from, genericMsg);
}

fun Personal.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

fun Personal.sendDestroy(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Destroy {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Personal.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Personal.getState(self): PersonalStore {
    return net.runGetMethod(self.address, "get_state");
}

fun Personal.getJettonData(self): JettonDataReply {
    return net.runGetMethod(self.address, "get_jetton_data");
}

fun Personal.getWalletAddress(self, owner: address): address {
    return net.runGetMethod(self.address, "get_wallet_address", [owner]);
}
