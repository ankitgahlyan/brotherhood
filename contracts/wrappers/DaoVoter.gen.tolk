/// Auto-generated wrapper for contract 'DaoVoter'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/dao/DaoVoter"

/// Storage `DaoVoterStore` is defined in `@contracts/dao/DaoVoter`
struct DaoVoter {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DaoVoter.fromStorage(
    storage: DaoVoterStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DaoVoter {
    val stateInit = ContractState {
        code: build("DaoVoter"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DaoVoter { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DaoVoter.fromAddress(address: address): DaoVoter {
    return DaoVoter { address };
}

/// Deploys the contract to the blockchain
fun DaoVoter.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DaoVoter.sendVoteProposalChild(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    voterOwner: address,
    vote: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VoteProposalChild { queryId, proposalId, voterOwner, vote },
    });
    return net.send(from, genericMsg);
}

fun DaoVoter.sendCleanupProposalVotes(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CleanupProposalVotes { queryId, proposalId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DaoVoter.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
