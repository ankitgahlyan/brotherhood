// Auto-generated wrapper for contract 'CityMap'
//
// This file is automatically generated by 'acton wrapper'
// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"
import "@contracts/common/messages"
import "@contracts/location/CityMap"

/// Storage `CityMapStore` is defined in `@contracts/location/CityMap`
struct CityMap {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun CityMap.fromStorage(
    storage: CityMapStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): CityMap {
    val stateInit = ContractState {
        code: build("CityMap"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return CityMap { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun CityMap.fromAddress(address: address): CityMap {
    return CityMap { address };
}

/// Deploys the contract to the blockchain
fun CityMap.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun CityMap.sendRegisterCityMember(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    cityName: string,
    sendExcessesTo: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RegisterCityMember { queryId, ownerAddress, cityName, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

fun CityMap.sendUnregisterCityMember(
    self,
    from: address,
    queryId: uint64,
    ownerAddress: address,
    cityName: string,
    sendExcessesTo: address?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: UnregisterCityMember { queryId, ownerAddress, cityName, sendExcessesTo },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun CityMap.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun CityMap.getCityName(self): string {
    return net.runGetMethod(self.address, "get_city_name");
}

fun CityMap.getLocationAddress(self): address {
    return net.runGetMethod(self.address, "get_location_address");
}

fun CityMap.getMembers(self): map<address, bool> {
    return net.runGetMethod(self.address, "get_members");
}
