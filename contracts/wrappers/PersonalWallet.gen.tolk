/// Auto-generated wrapper for contract 'PersonalWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/personalMinter/storage"

/// Storage `PriWalletStore` is defined in `@contracts/personalMinter/storage`
struct PersonalWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun PersonalWallet.fromStorage(
    storage: PriWalletStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): PersonalWallet {
    val stateInit = ContractState {
        code: build("PersonalWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return PersonalWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun PersonalWallet.fromAddress(address: address): PersonalWallet {
    return PersonalWallet { address };
}

/// Deploys the contract to the blockchain
fun PersonalWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.sendAskToTransfer(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    transferRecipient: address,
    sendExcessesTo: address?,
    customPayload: cell?,
    forwardTonAmount: coins,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x0f8a7ea5, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(transferRecipient)
        .storeAny(sendExcessesTo)
        .storeAny(customPayload)
        .storeAny(forwardTonAmount)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AskToTransfer.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.sendAskToBurn(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    sendExcessesTo: address?,
    customPayload: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: AskToBurn { queryId, jettonAmount, sendExcessesTo, customPayload },
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.sendInternalTransferStep(
    self,
    from: address,
    queryId: uint64,
    jettonAmount: coins,
    version: uint10,
    transferredAsCredit: bool,
    transferInitiator: address,
    sendExcessesTo: address?,
    forwardTonAmount: coins,
    forwardPayload: PayloadInline | PayloadInRef,
    config: SendParams = {},
): SendResultList {
    // build body cell manually, because some fields have @abi.clientType
    val bodyB = beginCell()
        .storeUint(0x178d4519, 32)
        .storeAny(queryId)
        .storeAny(jettonAmount)
        .storeAny(version)
        .storeAny(transferredAsCredit)
        .storeAny(transferInitiator)
        .storeAny(sendExcessesTo)
        .storeAny(forwardTonAmount)
        .storeAny(forwardPayload);
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: InternalTransferStep.fromSlice(bodyB.toSlice()),
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.sendHotUpgrade(
    self,
    from: address,
    additionalData: cell?,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HotUpgrade { additionalData, code },
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.sendUpgrade(
    self,
    from: address,
    walletUpgrade: bool,
    walletVersion: uint10,
    sender: address,
    newData: cell?,
    newCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Upgrade { walletUpgrade, walletVersion, sender, newData, newCode },
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.sendRequestUpgradeCode(
    self,
    from: address,
    sender: address,
    version: uint10,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: RequestUpgradeCode { sender, version },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun PersonalWallet.sendAny(
    self,
    from: address,
    body: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun PersonalWallet.getVersion(self): uint10 {
    return net.runGetMethod(self.address, "get_version");
}

fun PersonalWallet.getPersonalWalletState(self): PriWalletStore {
    return net.runGetMethod(self.address, "get_personal_wallet_state");
}

fun PersonalWallet.getWalletData(self): JettonWalletDataReply {
    return net.runGetMethod(self.address, "get_wallet_data");
}
