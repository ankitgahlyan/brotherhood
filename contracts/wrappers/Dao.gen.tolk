/// Auto-generated wrapper for contract 'Dao'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/dao/storage"

/// Storage `DaoStore` is defined in `@contracts/dao/storage`
struct Dao {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Dao.fromStorage(
    storage: DaoStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Dao {
    val stateInit = ContractState {
        code: build("Dao"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Dao { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Dao.fromAddress(address: address): Dao {
    return Dao { address };
}

/// Deploys the contract to the blockchain
fun Dao.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Dao.sendSubmitProposal(
    self,
    from: address,
    id: uint64,
    proposerOwner: address,
    targetAddr: address,
    targetMsg: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: SubmitProposal { id, proposerOwner, targetAddr, targetMsg },
    });
    return net.send(from, genericMsg);
}

fun Dao.sendResponseTotalAccounts(
    self,
    from: address,
    queryId: uint64,
    totalAccounts: uint33,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ResponseTotalAccounts { queryId, totalAccounts },
    });
    return net.send(from, genericMsg);
}

fun Dao.sendVoteProposal(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    voterOwner: address,
    oldVote: bool?,
    newVote: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VoteProposal { queryId, proposalId, voterOwner, oldVote, newVote },
    });
    return net.send(from, genericMsg);
}

fun Dao.sendCleanupProposalVotes(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CleanupProposalVotes { queryId, proposalId },
    });
    return net.send(from, genericMsg);
}

fun Dao.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Dao.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Dao.getDaoData(self): DaoStore {
    return net.runGetMethod(self.address, "get_dao_data");
}

fun Dao.getProposal(self): Proposal {
    return net.runGetMethod(self.address, "get_proposal");
}

fun Dao.getDaoVoterAddress(self, voterOwner: address): address {
    return net.runGetMethod(self.address, "get_dao_voter_address", [voterOwner]);
}
