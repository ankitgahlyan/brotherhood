/// Auto-generated wrapper for contract 'BaseFiWallet'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/fossFi/storage"

/// Storage `BaseFiWalletStore` is defined in `@contracts/fossFi/storage`
struct BaseFiWallet {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun BaseFiWallet.fromStorage(
    storage: BaseFiWalletStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): BaseFiWallet {
    val stateInit = ContractState {
        code: build("BaseFiWallet"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return BaseFiWallet { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun BaseFiWallet.fromAddress(address: address): BaseFiWallet {
    return BaseFiWallet { address };
}

/// Deploys the contract to the blockchain
fun BaseFiWallet.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun BaseFiWallet.sendTopUpTons(
    self,
    from: address,
    latestFiWalletCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons { latestFiWalletCode },
    });
    return net.send(from, genericMsg);
}

fun BaseFiWallet.sendInternalInvite(
    self,
    from: address,
    queryId: uint64,
    version: uint10,
    sender: address,
    invitor: address,
    latestFiWalletCode: cell,
    currentStorage: cell?,
    username: string,
    h3Cell: string,
    country: uint16,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: InternalInvite {
            queryId,
            version,
            sender,
            invitor,
            latestFiWalletCode,
            currentStorage,
            username,
            h3Cell,
            country,
        },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun BaseFiWallet.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}
