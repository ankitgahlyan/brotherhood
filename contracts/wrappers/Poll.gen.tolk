/// Auto-generated wrapper for contract 'Poll'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/dao/storage"

/// Storage `PollStore` is defined in `@contracts/dao/storage`
struct Poll {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Poll.fromStorage(
    storage: PollStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Poll {
    val stateInit = ContractState {
        code: build("Poll"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Poll { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Poll.fromAddress(address: address): Poll {
    return Poll { address };
}

/// Deploys the contract to the blockchain
fun Poll.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Poll.sendInitPoll(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: InitPoll { queryId },
    });
    return net.send(from, genericMsg);
}

fun Poll.sendResponseTotalAccounts(
    self,
    from: address,
    queryId: uint64,
    totalAccounts: uint33,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ResponseTotalAccounts { queryId, totalAccounts },
    });
    return net.send(from, genericMsg);
}

fun Poll.sendVoteProposal(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    voterOwner: address,
    oldVote: bool?,
    newVote: bool,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: VoteProposal { queryId, proposalId, voterOwner, oldVote, newVote },
    });
    return net.send(from, genericMsg);
}

fun Poll.sendCleanupProposalVotes(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CleanupProposalVotes { queryId, proposalId },
    });
    return net.send(from, genericMsg);
}

fun Poll.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Poll.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Poll.getPollData(self): PollStore {
    return net.runGetMethod(self.address, "get_poll_data");
}

fun Poll.getVoterAddress(self, voterOwner: address): address {
    return net.runGetMethod(self.address, "get_voter_address", [voterOwner]);
}
