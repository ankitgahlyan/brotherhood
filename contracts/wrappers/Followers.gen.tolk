/// Auto-generated wrapper for contract 'Followers'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/followers/messages"
import "@contracts/followers/store"

/// Storage `FollowingStore` is defined in `@contracts/followers/store`
struct Followers {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Followers.fromStorage(
    storage: FollowingStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Followers {
    val stateInit = ContractState {
        code: build("Following"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Followers { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Followers.fromAddress(address: address): Followers {
    return Followers { address };
}

/// Deploys the contract to the blockchain
fun Followers.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Followers.sendInitFollow(
    self,
    from: address,
    queryId: uint64,
    followerOwner: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: InitFollow { queryId, followerOwner },
    });
    return net.send(from, genericMsg);
}

fun Followers.sendUnfollow(
    self,
    from: address,
    queryId: uint64,
    initiator: address,
    followee: address,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Unfollow { queryId, initiator, followee },
    });
    return net.send(from, genericMsg);
}

fun Followers.sendHotUpgrade(
    self,
    from: address,
    additionalData: cell?,
    code: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: HotUpgrade { additionalData, code },
    });
    return net.send(from, genericMsg);
}

fun Followers.sendUpgrade(
    self,
    from: address,
    walletUpgrade: bool,
    walletVersion: uint10,
    sender: address,
    newData: cell?,
    newCode: cell?,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: Upgrade { walletUpgrade, walletVersion, sender, newData, newCode },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Followers.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Followers.getFollowingData(self): FollowingStore {
    return net.runGetMethod(self.address, "get_following_data");
}

fun Followers.getIsFollowing(self): bool {
    return net.runGetMethod(self.address, "get_is_following");
}
