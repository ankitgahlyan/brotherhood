/// Auto-generated wrapper for contract 'Holding'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/holding/messages"
import "@contracts/holding/store"

/// Storage `HoldingStore` is defined in `@contracts/holding/store`
struct Holding {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun Holding.fromStorage(
    storage: HoldingStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): Holding {
    val stateInit = ContractState {
        code: build("Holding"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return Holding { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun Holding.fromAddress(address: address): Holding {
    return Holding { address };
}

/// Deploys the contract to the blockchain
fun Holding.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun Holding.sendClaimDeferredPayment(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ClaimDeferredPayment { queryId },
    });
    return net.send(from, genericMsg);
}

fun Holding.sendCancelDeferredPayment(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: CancelDeferredPayment { queryId },
    });
    return net.send(from, genericMsg);
}

fun Holding.sendFallbackReclaim(
    self,
    from: address,
    queryId: uint64,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: FallbackReclaim { queryId },
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun Holding.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun Holding.getHoldingData(self): HoldingStore {
    return net.runGetMethod(self.address, "get_holding_data");
}
