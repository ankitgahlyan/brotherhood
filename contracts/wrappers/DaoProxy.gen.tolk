/// Auto-generated wrapper for contract 'DaoProxy'
///
/// This file is automatically generated by 'acton wrapper'
/// Do not edit manually — changes will be overwritten

import "@acton/build"
import "@acton/emulation/network"
import "@acton/testing/assert"

import "@contracts/common/messages"
import "@contracts/dao/storage"

/// Storage `DaoProxyStore` is defined in `@contracts/dao/storage`
struct DaoProxy {
    address: address
    stateInit: ContractState? = null
    toShard: AddressShardingOptions? = null
}

/// Creates a contract wrapper instance from the storage data
fun DaoProxy.fromStorage(
    storage: DaoProxyStore,
    toShard: AddressShardingOptions? = null,
    workchain: int8 = BASECHAIN,
): DaoProxy {
    val stateInit = ContractState {
        code: build("DaoProxy"),
        data: storage.toCell(),
    };
    val address = AutoDeployAddress { workchain, stateInit, toShard }.calculateAddress();
    return DaoProxy { address, stateInit, toShard };
}

/// Creates a contract wrapper instance from the address
fun DaoProxy.fromAddress(address: address): DaoProxy {
    return DaoProxy { address };
}

/// Deploys the contract to the blockchain
fun DaoProxy.deploy(self, from: address, config: SendParams = {}): SendResultList {
    if (self.stateInit == null) {
        Assert.fail("Cannot deploy a contract created with 'fromAddress' because it lacks state init for deployment");
    }
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: {
            workchain: self.address.getWorkchain(),
            stateInit: self.stateInit,
            toShard: self.toShard,
        },
    });
    return net.send(from, genericMsg);
}

fun DaoProxy.sendExecuteDaoProposal(
    self,
    from: address,
    queryId: uint64,
    proposalId: uint64,
    proposerOwner: address,
    expiresAt: uint32,
    walletLibRef: cell,
    targetMsg: cell,
    config: SendParams = {},
): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: ExecuteDaoProposal {
            queryId,
            proposalId,
            proposerOwner,
            expiresAt,
            walletLibRef,
            targetMsg,
        },
    });
    return net.send(from, genericMsg);
}

fun DaoProxy.sendTopUpTons(self, from: address, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body: TopUpTons {},
    });
    return net.send(from, genericMsg);
}

/// Send message to the contract with a custom body cell
fun DaoProxy.sendAny(self, from: address, body: cell, config: SendParams = {}): SendResultList {
    val genericMsg = createMessage({
        bounce: config.bounce,
        value: config.value,
        dest: self.address,
        body,
    });
    return net.send(from, genericMsg);
}

fun DaoProxy.getDaoProxyData(self): DaoProxyStore {
    return net.runGetMethod(self.address, "get_dao_proxy_data");
}
